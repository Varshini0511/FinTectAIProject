# Runtime deps for the app (this is what Streamlit Cloud installs).
# Heavy eval-only tools (DeepEval, Guardrails) live in requirements-dev.txt.

# --- Core agent (OpenAI-compatible API; default Groq) ---
openai>=1.40.0
python-dotenv>=1.0.1

# --- Database (accounts / cards / transactions / chat history) ---
psycopg[binary]>=3.1.0

# --- Observability ---
langsmith>=0.1.130

# --- Web UI ---
streamlit>=1.38.0

# --- Safety: PII redaction (Presidio + a small spaCy model that fits free-tier RAM) ---
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# --- Token / cost accounting ---
tiktoken>=0.7.0
